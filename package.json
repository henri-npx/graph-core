{
  "name": "shepherd",
  "license": "UNLICENSED",
  "scripts": {
    "codegen": "graph codegen",
    "build": "graph build",
    "deploy": "graph deploy --node https://api.thegraph.com/deploy/ henri-npx/shepherd",
    "create-local": "graph create --node http://localhost:8020/ henri-npx/shepherd",
    "remove-local": "graph remove --node http://localhost:8020/ henri-npx/shepherd",
    "deploy-local": "graph deploy --node http://localhost:8020/ --ipfs http://localhost:5001 henri-npx/shepherd"
  },
  "dependencies": {
    "@graphprotocol/graph-cli": "0.29.2",
    "@graphprotocol/graph-ts": "0.26.0",
    "ws": "^8.6.0"
  }
}
